// EditSODialog — การแก้ไขใบสั่งขายทำผ่าน SO state machine (cancel + create ใหม่)
// Component นี้ถูก stub ไว้ — ไม่แสดง UI

export function EditSODialog(_props: {
  isOpen: boolean;
  onClose: () => void;
  onUpdated: () => void;
  so?: unknown;
}) {
  return null;
}
