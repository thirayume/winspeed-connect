# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage
playwright-report/
test-results/

# Production
dist
dist-ssr
.output
.vercel

# Misc
.DS_Store
*.pem

# Debug
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Local env files
.env*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Project specific
winspeed-connect/
backend/node_modules/
WSSale-App/node_modules/
WSSale-App/dist/

Ref/

# TruckScale DB dumps (อย่า commit ข้อมูลจริง)
db-init/mysql/*.sql
db-init/mysql/*.sql.gz
users_credentials.csv

# Database backups - large files, never commit (repo is public)
deploy/onprem/backup/
deploy/cloud-vps/windows/remote-config.bat
deploy/cloud-vps/server/server-config.env
deploy/cloud-vps/.local-secrets/
*.bak
*.bacpac
*.trn

# Per-customer provisioning profiles (contain secrets)
wf-customers/

# Documentation & internal requirements (contain sensitive specs)
#
# เอกสารไม่เข้า git โดยปริยาย เพราะมีชื่อพนักงานและสเปกภายใน
# ยกเว้น pipeline ซึ่งเป็น "เครื่องมือ" ไม่ใช่ "เอกสาร" — ต้องอยู่ใน git
# ไม่งั้น gate ที่ซ่อมบนเครื่องหนึ่งจะไม่ถึงอีกเครื่อง (พบปัญหานี้ 03/09/2569)
# git ไม่ยอมให้ re-include ไฟล์ถ้าโฟลเดอร์แม่ถูก exclude จึงต้องไล่เปิดทีละชั้น
docs/*
!docs/enterprise/
docs/enterprise/*
!docs/enterprise/pipeline/

# ผลลัพธ์ที่สร้างใหม่ได้ ไม่ต้องเก็บ
docs/enterprise/pipeline/baselines/
docs/enterprise/pipeline/reports/
docs/enterprise/pipeline/diagrams/
docs/enterprise/pipeline/uicap/
docs/enterprise/pipeline/docgen/_legacy/
docs/enterprise/pipeline/last-run-report.md

# ที่รวมไฟล์สำหรับส่งให้ทีมแบบออฟไลน์ (Credentials.txt ฯลฯ) — ห้ามเข้า git เด็ดขาด
docs/_offline-share/
_docs_backup/

# บันทึกการประชุม / ไฟล์ทิ้งขว้าง — เคยหลุดขึ้น repo สาธารณะมาแล้ว (ล้างประวัติ 23/08/2569)
# เอกสารภายในที่มีชื่อพนักงาน ลูกค้า หรือ IP ภายใน ห้ามอยู่ในโค้ดเบส
_minutes.txt
*_minutes.txt
debug.txt
tmp.txt

# ฐานตั้งต้นที่ Setup-Replication.ps1 ดัมป์ออกมา — สร้างใหม่ได้เสมอ
deploy/truckscale-replica/baseline.sql

# สภาพ DSN ของเครื่องแต่ละเครื่อง ไม่ใช่ของโครงการ
deploy/truckscale-replica/dsn-backup/

# Python build artifacts — เคยมี IP ผู้ดูแลหลุดออกไปกับไฟล์ .pyc
__pycache__/
*.pyc
