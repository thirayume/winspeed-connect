---
documentId: "WF-RPT-003"
title: "แผนสร้างรายงาน TruckScale ใน App"
version: "v1.0"
status: Draft
owner: "Integration"
normative: false
---

# แผนสร้างรายงาน TruckScale ใน App

ทำให้ App ออกรายงานได้เหมือนต้นฉบับ Crystal Reports ของ TruckScale
บัญชีรายการที่สำรวจจากไฟล์จริงอยู่ใน [TRUCKSCALE-REPORT-INVENTORY](TRUCKSCALE-REPORT-INVENTORY.md)

## ข่าวดีจากการสำรวจ

รายงานทั้ง 41 ใบใช้ตารางรวมกันเพียง **6 ตาราง** และกระจุกอยู่ที่สามตารางแรกอย่างมาก

| ตาราง | ใช้ในกี่ใบ | ตัวผลักส่งอยู่แล้ว |
|---|---|---|
| `tblproduct_detail` | 36 | ✅ |
| `tblscale` | 30 | ✅ |
| `tbl_keyone` | 2 | ✅ |
| `tblcustomer` | 1 | ❌ |
| `tblproduct` | 1 | ❌ |
| `tbl_boat` | 1 | ❌ |

**เพิ่มอีกสามตารางในตัวผลัก แล้วข้อมูลบนคลาวด์จะพอสำหรับรายงานครบทั้ง 41 ใบ**
เป็นการเพิ่มชื่อในอาร์เรย์ `TABLES` ของ `push-agent/sync-agent.js` เท่านั้น
(ต้องยืนยันก่อนว่าทั้งสามมี PK เป็น auto-increment เหมือนสามตัวแรก)

## ของที่มีอยู่แล้วใน App

| ส่วน | สภาพ |
|---|---|
| `components/reports/ScaleReportsPage.tsx` | มี 3 มุมมอง — ตามวัน · ตามสูตรปุ๋ย · ตามเที่ยว |
| `components/reports/LegacyReportPdfModal.tsx` | ตัวเรนเดอร์ A4 พร้อมหัวเอกสารตั้งค่าได้ · วันที่ไทย · QR · สั่งพิมพ์ |
| `routes/truckscale.js` | มี endpoint อ่านข้อมูลชั่งแล้ว |

โครงพร้อม งานที่เหลือคือ**เพิ่มรายงาน ไม่ใช่สร้างระบบรายงานใหม่**

## จัดกลุ่มรายงานต้นฉบับ

### ก. เอกสารหน้างาน — พิมพ์ให้คนถือ (6 ใบ)

`RptSaYPan` (สายพาน) · `RptBULK` · `RptKlungSinKa` · `RptOther` · `RptSent` · `ReportKangChang`

> **แก้ข้อสรุปเดิม 31/08/2569** — เคยเขียนว่า "ทุกใบหน้าตาเดียวกัน ต่างที่หัวเรื่องกับเงื่อนไขกรอง"
> **ไม่จริง** เทียบชื่อฟิลด์ที่แต่ละ `.rpt` อ้างถึงจริง แล้วแยกได้เป็น **สามแบบ ไม่ใช่หนึ่ง**

| แบบ | ใบ | ฟิลด์ | ตารางที่อ่าน |
|---|---|---|---|
| 1 | `RptBULK` · `RptOther` | 19 | `tblproduct_detail` อย่างเดียว |
| 2 | `RptSaYPan` · `RptKlungSinKa` | 19 + `sto_text` | `tblproduct_detail` อย่างเดียว |
| 3 | `RptSent` · `ReportKangChang` | **39** | + `tblscale` (น้ำหนัก วันที่ คนขับ ทะเบียน `weight_Type`) |

แบบ 3 เป็น**เอกสารคนละใบ** ไม่ใช่ใบจ่ายสินค้าที่เปลี่ยนหัวเรื่อง — มีน้ำหนักเข้า/ออก/สุทธิ
วันเวลาเข้าออก ชื่อและที่อยู่คนขับ ประเภทลูกค้า ลาน ต้องออกแบบแยกต่างหาก

### ตัวแยกว่าใบไหนเป็นรายงานใด — `pd_pro_Godown`

ยืนยันกับข้อมูลจริง 550,054 แถว ค่าที่มีอยู่จริงบอกชื่อรายงานตรง ๆ:

| รายงาน | ค่าใน `pd_pro_Godown` | `sto_text` | แถว |
|---|---|---|---|
| `RptSaYPan` (สายพาน) | `สายพาน(B/B)` · `สายพาน(H/P)` | เชิงผสม · แฮนแพ็ค | 296,289 |
| `RptKlungSinKa` (คลังสินค้า) | `คลัง(B/B)` · `คลัง(H/P)` | เชิงผสม · แฮนแพ็ค | 96,361 |
| `RptBULK` | `โกดัง1`…`โกดัง6` (+ `4/1` `4/2` `5/1` `5/2`) | เทกอง ทั้งหมด | ~141,500 |
| `RptOther` (อื่น ๆ) | `ห้องกระสอบ` · `ท่าน้ำ` · `-` | ตามชื่อ | ~16,000 |

**ทำไมมีแค่สองใบที่อ่าน `sto_text`** — `RptSaYPan` กับ `RptKlungSinKa` ครอบคลุมโกดังสองแบบต่อใบ
(B/B = เชิงผสม, H/P = แฮนแพ็ค) จึงต้องแสดงว่าบรรทัดนี้เป็นแบบไหน
ส่วน `RptBULK` โกดัง 1-6 เป็น `เทกอง` หมด ไม่ต้องแยก และ `RptOther` ดูจากชื่อโกดังได้เลย

`sto_des` **ไม่ใช่หมายเหตุ** — มีแค่ค่า `'0'`/`'1'` เคยเอาไปแสดงเป็นหมายเหตุแล้วได้ตัวเลขลอย ๆ

**กลุ่มนี้ควรทำก่อน** — เป็นเอกสารที่ใช้ทุกวันและมีผลผูกพันกับลูกค้า
แบบ 1 กับ 2 ใช้คอมโพเนนต์เดียวกันได้ (ทำแล้ว) เหลือแบบ 3 ที่ต้องออกแบบใหม่

### ข. สรุปตามมิติ (26 ใบ)

แต่ละมิติมีสามแบบ — `Detail` (รายบรรทัด) · `Group` (สรุป) · `Group2` (สรุปอีกชั้น)

มิติที่มี: วันที่ · ลูกค้า · สินค้า · โกดัง · ลาน (Park) · ตั๋ว (Tua) · พาเลท (Palit) ·
ใบเคลื่อนย้าย (MoveBill) · ประเภทลูกค้า (Custype) · เรือ (Boat)

`ScaleReportsPage` ครอบคลุมสามมิติแล้ว (วัน · สูตรปุ๋ย · เที่ยว) เหลืออีกเจ็ดมิติ

### ค. ทะเบียนข้อมูลหลัก (5 ใบ)

`Report_Customer` · `Report_Driver` · `Report_Godown` · `Report_Goods` · `Report_KeyOne`

รายการข้อมูลหลักล้วน ๆ ความสำคัญต่ำสุด — App มีหน้าจอข้อมูลหลักของตัวเองอยู่แล้ว

### ง. เรือ (4 ใบ)

`Report_Boat` · `Report_BoatDetail` · `Report_BoatGroup` · `Report_BoatGroup2`

**ต้องถามก่อนว่ายังใช้อยู่ไหม** — ถ้าโรงงานไม่ได้รับของทางเรือแล้วก็ไม่ต้องทำ

## ลำดับที่เสนอ

| ลำดับ | งาน | เหตุผล |
|---|---|---|
| 1 | เพิ่ม 3 ตารางในตัวผลัก | ปลดล็อกข้อมูลให้ครบก่อน ไม่งั้นทำรายงานไปก็ไม่มีข้อมูล |
| 2 | ใบจ่ายสินค้า 1 ใบ (`RptSaYPan`) ให้เหมือนต้นฉบับ | ตรวจกับของจริงได้ทันที มีภาพต้นฉบับเทียบ |
| 3 | ขยายเป็นอีก 5 ใบในกลุ่ม ก | ใช้โครงเดียวกัน |
| 4 | เติมมิติที่เหลือในกลุ่ม ข | ทำทีละมิติ Detail ก่อน Group |
| 5 | กลุ่ม ค / ง | ตามที่ยังจำเป็น |

## การจับคู่ช่องของ RptSaYPan — ยืนยันกับข้อมูลจริงแล้ว

| ช่องบนเอกสาร | คอลัมน์ | หมายเหตุ |
|---|---|---|
| เลขที่ใบสั่งจ่าย | `tblproduct_detail.pd_pro_invoid` | คอมเมนต์ในตารางเขียนว่า "เลขที่ใบสั่งจ่าย" · เอกสารเก่าเป็นเลขรัน 6 หลัก (พบ 75,273 แถว) · **ใบที่ App สร้างจะเป็นเลข SO เช่น `I69-02423`** |
| วันที่ | `tblscale.Date_Out` | เก็บเป็นข้อความ `dd/mm/yyyy` พ.ศ. ไม่ใช่ชนิดวันที่ · ต้นฉบับพิมพ์เป็นชื่อเดือนไทยเต็ม |
| ชื่อร้านลูกค้า | `tblscale.one_cus_name` | |
| ทะเบียนรถ | `tblscale.one_car_regis` | |
| ปุ๋ยสูตร | `pd_pro_name` | ใบที่ App สร้างจะได้**รหัสสินค้า**ไม่ใช่ชื่อไทย เพราะ App เขียนรหัสลงไป |
| ตรา | `pd_pro_formula` | ใบที่ App สร้างเป็น `-` |
| จำนวน กส. | `pd_pro_bag` | |
| จำนวน ตัน | `pd_pro_wantWeight` | **เป็นตันจริง ไม่ต้องคำนวณ** — ตรวจแล้วเท่ากับ `bag × pd_pro_weight ÷ 1000` |
| หมายเหตุ | `pd_Destination` · `sto_des` | ยังไม่ยืนยัน — ตัวอย่างที่มีเป็น `-` ทั้งคู่ ต้องรอเอกสารจริงใบที่มีหมายเหตุ |
| เชื่อมหัวกับบรรทัด | `tblscale.one_num` = `tblproduct_detail.one_num` | ไม่ใช่ `tbl_keyone` — ตั๋วที่ทดสอบไม่มีแถวใน `tbl_keyone` เลย |

**ตรวจยอดข้ามตารางได้:** ผลรวม `bag × KgPerBag` ต้องเท่ากับ `tblscale.weight_net`
ตัวอย่างที่ทดสอบ 260 กระสอบ × 50 = 13,000 กก. ตรงกับ `weight_net` = 13,000 พอดี
ใช้เป็นการตรวจความถูกต้องของเอกสารได้เลย

### ทำแล้ว

`GET /api/truckscale/delivery-note/:sequence` — คืนหัวเอกสาร รายการสินค้า และยอดรวม
พร้อม `issueNoAll` เผื่อกรณีบรรทัดในใบเดียวมีเลขใบสั่งจ่ายไม่ตรงกัน (ไม่เลือกให้เงียบ ๆ)

### ยังไม่ทำ

หน้าจอพิมพ์ฝั่ง App — ต่อ `LegacyReportPdfModal` เข้ากับ endpoint นี้
และจัดวางให้ตรงต้นฉบับ (สามช่องเซ็น · บรรทัด "เหลือที่ตั๋ว" · หัวเรื่อง "สายพาน")

## เรื่องที่ต้องตัดสินก่อนเริ่มข้อ 2

1. **ตัวเลขต้องตรงกับต้นฉบับระดับไหน** — ตรงทุกหลัก หรือแค่รูปแบบเหมือน
   ถ้าต้องตรงทุกหลัก ต้องหาเอกสารจริงมาเทียบทีละใบ
2. **หัวกระดาษ** — ใช้หัวเอกสารของ App (ตั้งค่าได้อยู่แล้ว) หรือเลียนแบบต้นฉบับที่ไม่มีหัว
3. **เลขที่ใบสั่งจ่าย** — ต้นฉบับใช้ `000024` (6 หลักรันเลข) ต้องรู้ว่ามาจากคอลัมน์ไหน
   ยังไม่พบใน inventory ต้องเปิดดูข้อมูลจริง

## อ่านไฟล์ `.rpt` ได้แค่ไหน — สรุปหลังลองจริง

| อ่านได้ | วิธี |
|---|---|
| ชื่อตารางและฟิลด์ | ASCII ตรง ๆ ในไฟล์ |
| **ใบไหนอ่านฟิลด์อะไรบ้าง** | เทียบเซตของฟิลด์ระหว่างไฟล์ — นี่คือที่มาของ "สามแบบ" ข้างบน |
| **เงื่อนไขกรองจริง** | **ไม่ได้จาก `.rpt`** แต่ได้จาก `WorldFerth.exe` ซึ่งเก็บ SQL ไว้เป็นข้อความ |

**ข้อความภาษาไทยใน `.rpt` ยังอ่านไม่ออก** — เก็บเป็นรหัส 3 ไบต์ `22 <hi> <lo>`
(`hi` = `0x7a`/`0x7b` · `lo` = `0x42`–`0x7b`) พบ 37 รหัสที่ต่างกันใน `RptSaYPan.rpt`
ซึ่งสอดคล้องกับจำนวนอักขระไทยในเอกสารหนึ่งใบ แต่ยังจับคู่รหัส→อักขระไม่สำเร็จ
ลองแล้ว: TIS-620, UTF-16LE/BE, base-64 หกบิต, ออฟเซ็ตคงที่หลายค่า — ไม่มีอันไหนให้คำที่อ่านรู้เรื่อง

**ไม่เป็นอุปสรรค** เพราะข้อความบนเอกสารเป็นป้ายกำกับคงที่ ซึ่งเห็นได้จากภาพที่พิมพ์จริงอยู่แล้ว
สิ่งที่ต้องรู้จริง ๆ คือ *ฟิลด์ไหน* กับ *กรองด้วยอะไร* ซึ่งได้ครบแล้วทั้งคู่

### SQL ที่ขุดได้จาก `WorldFerth.exe`

```
select * from tblscale inner join tblproduct_detail
   ON tblscale.one_num = tblproduct_detail.one_num
 WHERE weight_Type = '2' and Date_Out2 >= ...
```
`weight_Type`: `2` = ชั่งครบแล้ว (403,652 แถว) · `0` = ค้างกลางทาง (266) · `1` = (15)
`one_w_type`: `ชั่งรับ` (239,649) · `ชั่งขาย` (164,284) — แยกใบรับเข้ากับใบขายออก
รายงานกลุ่ม ก ทั้งหมดอยู่ฝั่ง `ชั่งขาย` และ `weight_Type = '2'`

**ยังต้องการภาพที่พิมพ์จริงของแบบ 3** (`RptSent` · `ReportKangChang`) — สองใบนั้นมีข้อมูล
มากกว่าที่ภาพ `RptSaYPan` จะบอกได้ และเดาการจัดวางไม่ได้
